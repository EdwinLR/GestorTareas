@model GestorTareas.Web.Data.Entities.Project

@{
    ViewData["Title"] = "Detalles del Proyecto";
    ViewBag.ProjectId = Model.Id;
}

<link rel="stylesheet" href="~/css/style-icon.css" />

<h1>Detalles del Proyecto</h1>

<div>
    <h4>Proyecto</h4>
    <a asp-action="CreateCollaboratorsDetails" asp-route-id="@Model.Id" class="btn btn btn-outline-success">
                    <span class="icon-plus"></span> Añadir Colaboradores al Proyecto</a>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ProjectName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ProjectName)
        </dd>
        <dt class="col-sm-2">
            Convocatoria Relacionada
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Convocation.Summary)
        </dd>
    </dl>
</div>
<h4>Personas Asignadas</h4>
<table class="table" style="text-align:center;">
    <thead>
        <tr>
            <th scope="col">
                @Html.DisplayNameFor(model => model.ProjectAssigments)
            </th>
            <th scope="col">
                Nombre completo
            </th>
            <th scope="col">
                Correo Electrónico
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ProjectAssigments)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.User.FullName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.User.Email)
                </td>
            </tr>
        }
    </tbody>
</table>
<div>
    <a asp-action="Index" class="btn btn-outline-secondary">
        <span class="icon-undo2"></span>  Regresar a la lista
    </a>
</div>
