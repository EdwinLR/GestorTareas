@model GestorTareas.Web.Models.AddConvocationViewModel

@{
    ViewData["Title"] = "Agregar Convocatoria";
}

<link rel="stylesheet" href="~/css/style-icon.css" />
<link rel="stylesheet" href="~/css/estiloPaginas.css" />

<h1>Agregar Convocatoria</h1>
<br />

<h4>Convocatorias</h4>
<table class="table" style="text-align:center;">
    <thead>
        <tr>
            <th scope="col">
                @Html.DisplayNameFor(model => model.ConvocationId)
            </th>
            <th scope="col">
                <a>Eliminar</a>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.ConvocationDetails)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Convocation.Summary)
                </td>
                <td>
                    <a asp-action="DeleteConvocation" asp-route-id="@item.Id" class="btn btn-outline-danger">
                        <span class="icon-cancel-circle"></span>
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>
<hr />
<br />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AddConvocations" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="ConvocationId" class="control-label"></label>
                <select asp-for="ConvocationId" asp-items="Model.ConvocationList" class="form-control"></select>
                <span asp-validation-for="ConvocationId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Agregar" class="btn btn-outline-success" />
                <a asp-action="Create" class="btn btn-outline-secondary">
                    <span class="icon-undo2"></span> Regresar
                </a>
            </div>
        </form>
    </div>
</div>



@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }

