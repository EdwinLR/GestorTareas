<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="GestorTareas.Prism.Views.AdminHomePage" BackgroundColor="#E9E9E9">

    <StackLayout>
        <ListView x:Name="MenuItems" Margin="10,5,10,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ImageCell Text="{Binding Item}"
                               Detail="{Binding Detail}"
                               ImageSource="{Binding Image}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Text="Configuración" Margin="20,0,20,10" BackgroundColor="Transparent" TextColor="Black"/>
    </StackLayout>
</ContentPage>